---
title: "Placeholder Case Study Two"
slug: "_placeholder-2"
summary: "A second placeholder entry to verify the work list handles multiple entries and sorts them by date."
tech: ["NestJS", "PostgreSQL"]
date: "2023-06-01"
featured: false
---

## Problem

A second dummy entry, dated earlier than `_placeholder-1`, to check that
`getAllWork()` sorts multiple case studies by `date` (newest first).

## Decision

- Kept short — this file is plumbing, not content.
- Different `tech` list and `date` than the first placeholder, to make sorting
  and per-entry data visibly distinct.

## Result

Both placeholders render independently at their own `/work/[slug]` route.
