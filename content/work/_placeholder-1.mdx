---
title: "Placeholder Case Study One"
slug: "_placeholder-1"
summary: "A placeholder entry used to verify the MDX pipeline — routing, frontmatter parsing, and rendering — before the real case studies are written."
tech: ["Next.js", "TypeScript", "MDX"]
date: "2024-01-01"
featured: false
---

## Problem

This is placeholder body content. It exists only to confirm that
`/work/_placeholder-1` renders correctly from an `.mdx` file.

## Decision

- Confirms frontmatter parsing works end to end (`title`, `summary`, `tech`, `date`).
- Confirms `generateStaticParams` picks up this slug at build time.
- Confirms standard markdown (headings, lists, **bold**, `inline code`) renders.

## Result

If you can read this on a live page, the MDX pipeline works. Real case study
content — including the flagship payroll system write-up — lands in PBW-18.
